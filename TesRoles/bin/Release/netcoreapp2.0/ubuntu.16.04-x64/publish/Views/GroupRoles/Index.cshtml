@model IEnumerable<TesRoles.Models.GroupRoles>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.roles)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.ApplicationGroup)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.roles)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApplicationGroup.ApplicationGroupId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GroupRolesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GroupRolesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GroupRolesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
